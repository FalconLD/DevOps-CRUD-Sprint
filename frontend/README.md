# Carpeta: `frontend/` — OWNER Frontend Engineer

Rama: `feature/frontend`

Lee: [Documentacion/Roles/FrontendEngineer.md](../Documentacion/Roles/FrontendEngineer.md)

## Debes crear aquí

- `index.html` — formulario (nombre, correo, categoría) + tabla
- JS con Fetch a **`/api/visitantes`** (rutas relativas, sin IPs)
- `Dockerfile` — imagen que sirva estáticos en el **puerto 80** interno
- Solo el bloque `services.frontend` en `docker-compose.yml` si hace falta ajustarlo

## No editar

`backend/`, `database/`, `nginx/`, `.github/`
